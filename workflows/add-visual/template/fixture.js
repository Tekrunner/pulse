/** Declared-schema synthetic rows used to author and test this neutral template. */
export const FIXTURE_ROWS = Object.freeze([
  Object.freeze({ label: "First", value: 12 }),
  Object.freeze({ label: "Second", value: 18 }),
  Object.freeze({ label: "Third", value: 15 }),
]);

export const CONSUMER_SCHEMA = Object.freeze({ label: "string", value: "number" });
