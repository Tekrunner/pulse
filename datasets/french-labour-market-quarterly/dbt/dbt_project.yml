name: french_labour_market_quarterly
version: 1.0.0
config-version: 2
profile: pulse_dataset
model-paths: [models]
models:
  french_labour_market_quarterly:
    +materialized: external
    +location: "{{ var('external_location') }}"
    +format: parquet
