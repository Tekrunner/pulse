name: french_gdp_dollar_decomposition
version: 1.0.0
config-version: 2
profile: pulse_dataset
model-paths: [models]
models:
  french_gdp_dollar_decomposition:
    +materialized: external
    +location: "{{ var('external_location') }}"
    +format: parquet
